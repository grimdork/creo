[template]
name=web
description=Go HTTP server with health endpoint and OCI
language=go
files=main.go.tmpl, fiat.tmpl, Dockerfile.tmpl

[vars]
PORT=8080
OCI_REPO=ghcr.io/$PROJECT
GOLANG_VERSION=1.24-alpine
ALPINE_VERSION=3.21
