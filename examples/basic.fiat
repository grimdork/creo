# Minimal Go project.
#
# Running "creo" here builds a binary named after the parent directory.
# The Go language mode fills in bin=$BUILDDIR/<dirname>, cmd=go build ...,
# and sources=*.go automatically.
#
# Usage:  creo
#         creo -v build
#         creo -l

build: go
